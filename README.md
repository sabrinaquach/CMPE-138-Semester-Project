Run the program using npm run dev
Go to http://localhost:3000/ for demo

For your local setup, create an .env file and fill in missing information in .env.example.
To verify databse access use mysql -u <your_db_username> -p cmpe138_project -e "SHOW TABLES;"


